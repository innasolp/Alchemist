@using Alchemist.Product.Import.WebApp.Infrastructure
@using Alchemist.Product.Interfaces
@using System.Text.Json
@model Alchemist.Product.Import.WebApp.Models.ShopSettingTabsModel

@functions {
    public object GetServiceData(ShopSettingType shopSettingType) => JsonSerializer.Serialize(new { shopId = Model.ShopId, shopSettingType = (int)shopSettingType });
}

@{
    var selectedShopSettings = Model.GetShopSettingsByType(Model.SelectedSettingsTab);
}
<hr />
<div class="text-center">
    <div class="flex">
        <div>
            <div class="menu" id="menuDiv">
                <div class="@ViewHelper.GetAnchorMenuItemStyle(Model.SelectedSettingsTab, ShopSettingType.Product)">
                    <a class="display-8  menu_header"
                           onclick="saveShopSettings($('#settingsForm'),(data)=> postData('/Home/SetShopSettings', @GetServiceData(ShopSettingType.Product), (r)=> {if(r) location.reload(); }))">
                        @ModelHelper.ShopSettingTypeNames[ShopSettingType.Product]
                    </a>
                </div>
                <div class="@ViewHelper.GetAnchorMenuItemStyle(Model.SelectedSettingsTab, ShopSettingType.Category)">
                    <a class="display-8  menu_header"
                           onclick="saveShopSettings($('#settingsForm'),(data)=> postData('/Home/SetShopSettings',  @GetServiceData(ShopSettingType.Category), (r)=> {if(r) location.reload(); }))">
                        @ModelHelper.ShopSettingTypeNames[ShopSettingType.Category]
                    </a>
                </div>
            </div>
            <div>
                @await Html.PartialAsync($"~/Views/Home/ShopSettings.cshtml", selectedShopSettings);
            </div>
        </div>
    </div>
</div>

