<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\ShopImport.ServiceHash\ShopImport.KeyHash.csproj" />
  </ItemGroup>
	
	
	<ItemGroup>
		<PackageReference Include="System.IO.Hashing" Version="9.0.13" />
	</ItemGroup>

	<Target Name="PostBuild" AfterTargets="PostBuildEvent">
		<Exec Command="set importDir=$(SolutionDir)\Import\ServiceState\KeyHash\XXHash64&#xD;&#xA;if not exist &quot;%25importDir%25&quot; mkdir  &quot;%25importDir%25&quot;&#xD;&#xA;xcopy /E /Y  &quot;$(TargetDir)&quot; &quot;%25importDir%25&quot;" />
	</Target>

</Project>
